# Enrichment Archive

This folder contains the previous enrichment pipeline generation:

- `orchestrator.mjs`
- `watchdog.mjs`
- `watchdog-keepalive.mjs`
- legacy workers under `legacy-workers/`

The active pipeline uses `scripts/enrichment/agents/*`,
`scripts/enrichment/queue.mjs`, `scripts/ops/*`, and the launchd templates
under `infra/local/launchd/`.
